import React from 'react';

const Header = (props: any) => {
    return (
        <header className={props.theme}>
            <h1>TypeScriptin'</h1>
        </header>
    );
}

export default Header;